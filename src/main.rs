use octocrab::{self, params::users};

#[tokio::main]
async fn main() {
	// let crab = octocrab::Octocrab::builder()
	// 	.add_header(http::, format!("Bearer {}").as_str()
	// );

	// let octocrab = octocrab::instance().;
	// let response = octocrab.users("hyperupcall");

	// let repos = response.repos();
	// let page: u32 = 0;
	// // loop {
	// let r = repos
	// 	.per_page(100)
	// 	.page(page)
	// 	.r#type(users::repos::Type::All)
	// 	.send()
	// 	.await
	// 	.unwrap();
	// // }
	// for thing in &r {
	// 	println!("a: {}", thing.name);
	// }
	// let orgs = octocrab.orgs("hyperupcall");
	// orgs.
}
